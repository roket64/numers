/// Struct for represent the solution of linear congruence ax = k (mod modulo)
pub struct CongrSolution {}

/// Struct that represents linear congruence `ax = n (mod m)`, where `coefficient` represents `a`, \
/// `modulo` represents `m`.
pub struct Congruence {
    coefficient: u32,
    n: u32,
    modulo: u32,
}

impl Congruence {
    pub fn solve() {
        todo!()
    }
}
